import React, {useState, useEffect} from 'react';

interface SearchAllPendingProps {
    onClick: ( ) => void
}

export const SearchAllPending : React.FC = () => {

    return(
        <div>
        </div>
    )
    
}
