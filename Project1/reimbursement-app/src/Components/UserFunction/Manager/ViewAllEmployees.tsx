import React, {useState, useEffect} from 'react';

interface ViewAllEmployeesProps {
    onClick: ( ) => void
}

export const ViewAllEmployees : React.FC = () => {

    return(
        <div>
        </div>
    )
    
}